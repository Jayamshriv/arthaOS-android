package com.jayam.aarthaos.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand ─────────────────────────────────────────────────────────────────
val Saffron        = Color(0xFFE87B2E)
val SaffronLight   = Color(0xFFFDF0E6)
val SaffronDim     = Color(0x33E87B2E)   // 20% opacity — icon backgrounds
val Gold           = Color(0xFFF5C842)

// ── Dark surfaces ─────────────────────────────────────────────────────────
val SurfaceBlack   = Color(0xFF1A1612)   // app background dark
val SurfaceDark    = Color(0xFF2C2420)   // cards on dark
val SurfaceElevated= Color(0xFF3D3028)   // elevated cards, bottom sheet
val SurfaceBorder  = Color(0xFF4A3A2E)   // subtle borders dark

// ── Light surfaces ────────────────────────────────────────────────────────
val OffWhite       = Color(0xFFFAF6EF)   // app background light
val WarmWhite      = Color(0xFFF0EAE0)   // cards on light
val WarmBorder     = Color(0xFFE8DECE)   // borders light

// ── Text ──────────────────────────────────────────────────────────────────
val TextPrimaryDark   = Color(0xFFF0EAE0)
val TextSecondaryDark = Color(0xFF8A7A6A)
val TextPrimaryLight  = Color(0xFF1A1612)
val TextSecondaryLight= Color(0xFFA07850)

// ── Semantic ──────────────────────────────────────────────────────────────
val Income   = Color(0xFF4CAF50)   // green — credit/income amounts
val Expense  = Color(0xFFFF6B6B)   // soft red — debit amounts  (dark mode)
val ExpenseL = Color(0xFFC0392B)   // deeper red — debit amounts (light mode)
val Warning  = Color(0xFFF5C842)   // gold — budget warnings
val Danger   = Color(0xFFE53935)   // red — budget exceeded

// ── Category colours ─────────────────────────────────────────────────────
val CatFood        = Color(0xFF4CAF50)
val CatTravel      = Color(0xFF2196F3)
val CatShopping    = Color(0xFFFF9800)
val CatBills       = Color(0xFF9C27B0)
val CatEntertain   = Color(0xFFE91E63)
val CatHealth      = Color(0xFF00BCD4)
val CatEducation   = Color(0xFF3F51B5)
val CatSalary      = Color(0xFF66BB6A)
val CatInvestments = Color(0xFFFFC107)
val CatOther       = Color(0xFF795548)